import { createStore } from 'vuex';
import courses from '@/store/modules/courses';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    courses,
  },
});
